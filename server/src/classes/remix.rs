use std::error::Error;

pub async fn remix() -> Result<(), Box<dyn Error + Send + Sync>> {

    // anade a un feed 

    // algo con remix + feeds y publicando xcopy estilo creando otro NFT para el mercado 
}
